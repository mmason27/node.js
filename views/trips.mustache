<!DOCTYPE html>
<html lang="en">
<head>
    <link rel="stylesheet" href="styles.css">
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Trip Planning App</title>
</head>
<body>
            {{#allTrips}}
    <h2>Welcome back, {{username}}</h2>
    <h3>Your Upcoming Trips</h3>
        <div class="tripList">
            <form method="POST" action="/delete-trip">
            <input type="hidden" value={{tripId}} name="tripId"/>
            <p>Trip to: {{title}}</p>
            <div class="tripImage"><img src='{{image}}' class="inputImage"></div>
            <div class=dates>
            Departure: {{departureDate}} - Return: {{returnDate}}</div>
            <br>
            <button>Delete</button>
            </form>
            <br>
            <a href="/add-trip">Add Trip</a>
         </div>
            {{/allTrips}}

            {{^allTrips}}
            <p>
            You have no trips at this time.
            </p>
            <a href="/add-trip">Add Trip</a>
            {{/allTrips}}
</body>
</html>